# modmail_discussion.py

Script to extract URLs from a specific modmail conversation.

## Overview

Reads a configured modmail conversation ID, extracts links posted by human moderators (ignoring bot messages), and prints them.

## Usage

```bash
python modmail_discussion.py
```

Requires `CONVERSATION_ID` set in the environment/config.
