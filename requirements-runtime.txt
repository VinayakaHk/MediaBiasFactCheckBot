# Runtime-only dependencies for the containerised bot.
#
# Derived from requirements.txt by dropping packages that are only used by
# tests/ (torch, torchvision, easyocr, opencv-python-headless, scikit-image,
# scipy, undetected-chromedriver, and their transitive deps) or are developer
# utilities (pip-review). Pins match requirements.txt so builds are reproducible.
#
# When running anything that needs the heavy ML stack (e.g. tests/ppxNews.test.py),
# use the full requirements.txt instead.

# --- Reddit -----------------------------------------------------------------
praw==7.8.1
prawcore==2.4.0
update-checker==0.18.0
websocket-client==1.9.0

# --- HTTP / requests --------------------------------------------------------
requests==2.32.5
charset-normalizer==3.4.4
idna==3.11
urllib3==2.6.3
certifi==2026.1.4

# --- Config / env -----------------------------------------------------------
python-dotenv==1.2.1

# --- MongoDB ----------------------------------------------------------------
pymongo==4.16.0
dnspython==2.8.0

# --- Selenium stack (Firefox + geckodriver scraping in src/perplexity.py) ---
selenium==4.41.0
trio==0.33.0
trio-websocket==0.12.2
wsproto==1.3.2
h11==0.16.0
outcome==1.3.0.post0
sniffio==1.3.1
sortedcontainers==2.4.0
attrs==25.4.0
PySocks==1.7.1

# --- Markdown conversion (Perplexity output -> Reddit markdown) -------------
markdownify==1.2.2
beautifulsoup4==4.14.3
soupsieve==2.8.3
six==1.17.0

# --- PyVirtualDisplay path in src/llm_automation.py (guarded by arch check) -
PyVirtualDisplay==3.0

# --- Flask keep_alive.py (optional keepalive service) -----------------------
Flask==3.1.3
blinker==1.9.0
click==8.3.1
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.6

# --- RSS parsing in auto_post_news.py (missing from requirements.txt!) ------
feedparser==6.0.11

# --- Misc -------------------------------------------------------------------
packaging==26.0
typing_extensions==4.15.0
